# GitHub write credentials

This directory is a local mount point only. Keep either a least-privilege
GitHub App `private-key.pem` or a fine-grained `token` here; both are ignored.
Never reuse the Phase 3 read credential files. Phase 4 flags remain disabled by
default, and no real write is part of local validation.
