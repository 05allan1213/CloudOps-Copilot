apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysql
  labels:
    {{- include "cloudops-kind-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: database
spec:
  serviceName: mysql
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: mysql
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mysql
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: database
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: mysql
          image: {{ include "cloudops-kind-platform.image" .Values.image | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy | quote }}
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.root.secretName | quote }}
                  key: {{ .Values.database.root.passwordKey | quote }}
            - name: MYSQL_DATABASE
              value: {{ .Values.database.name | quote }}
            - name: CLOUDOPS_API_USER
              value: {{ .Values.database.identities.api.user | quote }}
            - name: CLOUDOPS_API_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.identities.api.secretName | quote }}
                  key: {{ .Values.database.identities.api.passwordKey | quote }}
            - name: CLOUDOPS_WORKER_USER
              value: {{ .Values.database.identities.worker.user | quote }}
            - name: CLOUDOPS_WORKER_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.identities.worker.secretName | quote }}
                  key: {{ .Values.database.identities.worker.passwordKey | quote }}
            - name: CLOUDOPS_MIGRATE_USER
              value: {{ .Values.database.identities.migrate.user | quote }}
            - name: CLOUDOPS_MIGRATE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.identities.migrate.secretName | quote }}
                  key: {{ .Values.database.identities.migrate.passwordKey | quote }}
            - name: CLOUDOPS_BASELINE_USER
              value: {{ .Values.database.identities.baseline.user | quote }}
            - name: CLOUDOPS_BASELINE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.identities.baseline.secretName | quote }}
                  key: {{ .Values.database.identities.baseline.passwordKey | quote }}
          ports:
            - name: mysql
              containerPort: 3306
              protocol: TCP
          readinessProbe:
            exec:
              command: ["sh", "-ec", "mysqladmin ping -h 127.0.0.1 -u root -p\"$MYSQL_ROOT_PASSWORD\" --silent"]
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            exec:
              command: ["sh", "-ec", "mysqladmin ping -h 127.0.0.1 -u root -p\"$MYSQL_ROOT_PASSWORD\" --silent"]
            initialDelaySeconds: 60
            periodSeconds: 20
            timeoutSeconds: 5
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/mysql
            - name: bootstrap-identities
              mountPath: /docker-entrypoint-initdb.d/10-cloudops-identities.sh
              subPath: 10-cloudops-identities.sh
              readOnly: true
      volumes:
        - name: bootstrap-identities
          configMap:
            name: cloudops-mysql-bootstrap
            defaultMode: 0444
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: {{ .Values.persistence.size | quote }}
        {{- if .Values.persistence.storageClassName }}
        storageClassName: {{ .Values.persistence.storageClassName | quote }}
        {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: mysql
  labels:
    {{- include "cloudops-kind-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mysql
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
      protocol: TCP
