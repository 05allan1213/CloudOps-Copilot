name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  server-probe:
    name: server-probe checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"
          cache-dependency-path: server-monitor/server-probe/go.sum

      - name: Install goimports
        run: go install golang.org/x/tools/cmd/goimports@latest

      - name: Goimports
        working-directory: server-monitor/server-probe
        run: test -z "$(goimports -l .)"

      - name: Test
        working-directory: server-monitor/server-probe
        run: go test ./...

      - name: Vet
        working-directory: server-monitor/server-probe
        run: go vet ./...

  server-web:
    name: server-web checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"
          cache-dependency-path: server-monitor/server-web/go.sum

      - name: Install goimports
        run: go install golang.org/x/tools/cmd/goimports@latest

      - name: Goimports
        working-directory: server-monitor/server-web
        run: test -z "$(goimports -l .)"

      - name: Test
        working-directory: server-monitor/server-web
        run: go test ./...

      - name: Vet
        working-directory: server-monitor/server-web
        run: go vet ./...

      - name: NLU Evaluation
        working-directory: server-monitor/server-web
        run: |
          set -o pipefail
          go test ./internal/copilot/nlu/eval/ -run TestEvaluate -v -count=1 2>&1 | tee nlu-eval-report.txt

      - name: RAG Evaluation
        working-directory: server-monitor/server-web
        run: |
          set -o pipefail
          go test ./internal/copilot/runbook/eval/ -run TestRAGEval -v -count=1 2>&1 | tee rag-eval-report.txt


  alert-service:
    name: alert-service checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"
          cache-dependency-path: server-monitor/alert-service/go.sum

      - name: Install goimports
        run: go install golang.org/x/tools/cmd/goimports@latest

      - name: Goimports
        working-directory: server-monitor/alert-service
        run: test -z "$(goimports -l .)"

      - name: Test
        working-directory: server-monitor/alert-service
        run: go test ./...

      - name: Vet
        working-directory: server-monitor/alert-service
        run: go vet ./...

  pkg:
    name: pkg checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"
          cache-dependency-path: server-monitor/pkg/go.sum

      - name: Install goimports
        run: go install golang.org/x/tools/cmd/goimports@latest

      - name: Goimports
        working-directory: server-monitor/pkg
        run: test -z "$(goimports -l .)"

      - name: Test
        working-directory: server-monitor/pkg
        run: go test ./...

      - name: Vet
        working-directory: server-monitor/pkg
        run: go vet ./...

  frontend:
    name: frontend build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: server-monitor/frontend/package-lock.json

      - name: Install dependencies
        working-directory: server-monitor/frontend
        run: npm ci

      - name: Build
        working-directory: server-monitor/frontend
        run: npm run build

  prometheus:
    name: prometheus config checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check Prometheus config
        run: |
          docker run --rm \
            --entrypoint promtool \
            -v "${{ github.workspace }}/server-monitor/docker/prometheus.yml:/etc/prometheus/prometheus.yml:ro" \
            -v "${{ github.workspace }}/server-monitor/docker/alerts.yml:/etc/prometheus/rules/alerts.yml:ro" \
            prom/prometheus:v2.51.0 \
            check config /etc/prometheus/prometheus.yml

      - name: Check Prometheus rules
        run: |
          docker run --rm \
            --entrypoint promtool \
            -v "${{ github.workspace }}/server-monitor/docker/alerts.yml:/etc/prometheus/alerts.yml:ro" \
            prom/prometheus:v2.51.0 \
            check rules /etc/prometheus/alerts.yml

      - name: Check AI quality alert rules
        run: |
          docker run --rm \
            --entrypoint promtool \
            -v "${{ github.workspace }}/server-monitor/docker/copilot-ai-quality-rules.yml:/etc/prometheus/rules/copilot-ai-quality-rules.yml:ro" \
            prom/prometheus:v2.51.0 \
            check rules /etc/prometheus/rules/copilot-ai-quality-rules.yml

  helm:
    name: helm chart checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: azure/setup-helm@v4
        with:
          version: v3.14.4

      - name: Lint chart
        run: helm lint server-monitor/charts/server-monitor

  docker-build:
    name: docker build checks
    runs-on: ubuntu-latest
    needs:
      - server-probe
      - server-web
      - alert-service
      - pkg
      - frontend
      - prometheus
      - helm
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Build probe image
        uses: docker/build-push-action@v5
        with:
          context: ./server-monitor
          file: ./server-monitor/server-probe/Dockerfile
          push: false
          tags: server-probe:ci

      - name: Build web image
        uses: docker/build-push-action@v5
        with:
          context: ./server-monitor
          file: ./server-monitor/server-web/Dockerfile
          push: false
          tags: server-web:ci

      - name: Build alert-service image
        uses: docker/build-push-action@v5
        with:
          context: ./server-monitor
          file: ./server-monitor/alert-service/Dockerfile
          push: false
          tags: alert-service:ci

  push-images:
    name: push images
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs:
      - docker-build
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push probe
        uses: docker/build-push-action@v5
        with:
          context: ./server-monitor
          file: ./server-monitor/server-probe/Dockerfile
          push: true
          tags: |
            05allan1213/server-probe:latest
            05allan1213/server-probe:${{ github.sha }}

      - name: Build and push web
        uses: docker/build-push-action@v5
        with:
          context: ./server-monitor
          file: ./server-monitor/server-web/Dockerfile
          push: true
          tags: |
            05allan1213/server-web:latest
            05allan1213/server-web:${{ github.sha }}

      - name: Build and push alert-service
        uses: docker/build-push-action@v5
        with:
          context: ./server-monitor
          file: ./server-monitor/alert-service/Dockerfile
          push: true
          tags: |
            05allan1213/alert-service:latest
            05allan1213/alert-service:${{ github.sha }}
