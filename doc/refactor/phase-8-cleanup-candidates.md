# Phase 8 cleanup decision matrix

Current source and callers are authoritative. `Tests` lists deterministic evidence available locally; credentialed runtime traffic was not observed.

| Candidate | Current callers | Workbench parity | Tests | Runtime/traffic evidence | Rollback | Decision |
| --- | --- | --- | --- | --- | --- | --- |
| Alerts / Alert History pages, routes, store and APIs | `AlertsPage`, `AlertHistoriesPage`, monitor store, diagnosis triggers, Copilot `alert.history` tool | Partial Signal/Incident display; no history deep-link or diagnosis-trigger parity | Frontend route contract; Go regression | No staging route/API traffic inventory | Retain V1 routes, Redis/MySQL history and pages | `BLOCKED_BY_STAGING_EVIDENCE` |
| Diagnosis list/detail, report and feedback | Three pages/components, alert/history/node triggers, PendingAction creation, MySQL report/feedback repositories | Agent investigation does not migrate report IDs, feedback or legacy deep links | Full Go regression; frontend route contract | No migrated-link or feedback traffic evidence | Keep DiagnosisReport tables/routes/worker | `RETAIN_COMPATIBILITY` |
| Copilot chat, session and history | Copilot page, Redis session store, classifier/tool/summary service | Workbench has no general chat/session or Incident follow-up implementation | Full Go/frontend regression | No product convergence approval or session traffic inventory | Keep `/api/v1/copilot/**` and Redis sessions | `RETAIN_COMPATIBILITY` |
| Actions / PendingAction pages and APIs | Diagnosis detail, Actions pages, admin handlers, direct K8s executor, AuditLog | V2 remediation differs in repository/path/policy and does not replace legacy action data | Route/admin contract and full Go regression | No staging RBAC/audit parity evidence | Keep admin API, state machine, tables and executor flags | `DEPRECATE_ONLY` |
| Audit Logs page and API | Admin page, action repository/service | Timeline is Incident-scoped and not an approved compliance replacement | Route/admin contract and full Go regression | No compliance mapping or retention approval | Keep AuditLog table and admin API | `RETAIN_COMPATIBILITY` |
| Standalone Kubernetes dashboard/client | 12 pages, global search, host detail, YAML viewer, Workbench resource summaries | Workbench is typed/bounded and intentionally omits general operator views | Frontend route contract; K8s adapter/handler regression | No standalone operator traffic inventory | Keep typed GET routes and pages; write remains separately gated | `RETAIN_COMPATIBILITY` |
| General V2 Incident/Signal/Timeline/Evidence DTOs | V2 APIs, Agent/change/delivery services and possible external clients; Workbench uses separate allowlists | Workbench is not a contract-compatible DTO replacement | Workbench sensitive-field and handler contracts | External caller inventory unavailable | Keep general DTOs and authenticated V2 routes | `RETAIN_COMPATIBILITY` |
| Global `/ws/alerts` stream | App shell, header, K8s Events page, host/diagnosis/action notifiers | Incident SSE does not carry global alert/host/diagnosis/action/K8s events | WebSocket auth and frontend no-URL-token tests | External subscriber/soak evidence unavailable | Route/messages retained; bearer subprotocol replaces URL credential | `BLOCKED_BY_STAGING_EVIDENCE` |
| Legacy services/repositories/adapters | V1 ingestion, Kafka diagnosis, Redis sessions/cache, action/audit and K8s clients | V2 has no outbox relay/inbox cutover or data backfill | Four-module full matrix and MySQL V2 regression | No production cutover or queue/data parity evidence | Keep processes, tables and feature flags | `RETAIN_COMPATIBILITY` |
| Compose, raw YAML, Helm and duplicate observability definitions | README/Makefile/CI/local setup and current deployment paths | No official-operator/Loki/Tempo/Collector replacement is deployed in this repo | Compose, Helm, YAML and kubeconform checks | No deployment-source cutover evidence | Retain all deployment paths; harden validation only | `BLOCKED_BY_STAGING_EVIDENCE` |
| Duplicate indirect `go.yaml.in/yaml/v3` requirement | No distinct module caller; same version already direct-required | Not applicable | `go mod tidy -diff`, four-module build/test/lint | Static module graph is sufficient | Re-run `go mod tidy` | `DELETE` |
| Tracked generated kind kubeconfig | Local setup regenerates it; no source code requires a committed credential | Not applicable | Credential structure scan; Git ignore check | Static evidence proves embedded client key | Run `make k8s-setup`; never restore credential to Git | `DELETE` |

No legacy table or historical record is dropped. All destructive business cleanup remains deferred until the missing traffic, subscriber, compliance, parity and rollback evidence exists.
